# Sample (self) artifacts

Redacted-free public samples from the self-audit of `BoozeLee` / `Bakery-street-project`:

| File | Purpose |
|---|---|
| `BoozeLee-report.pdf` | Paid-tier PDF shape |
| `BoozeLee-inventory.csv` | Row-level CSV shape |
| `BoozeLee-report.md` | Plain-text preview |

Full set regenerates with:

```bash
uv run graveyard scan --owner BoozeLee --out reports
```
